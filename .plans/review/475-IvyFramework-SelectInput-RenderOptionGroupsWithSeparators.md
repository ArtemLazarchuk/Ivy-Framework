# SelectInput: Option Group Separators - Review Checklist

- [ ] Open the SelectInput sample app and verify thin horizontal separators appear between option groups in the Select (dropdown) variant
- [ ] Verify no separator appears before the first group
- [ ] Verify ungrouped options (no `Group` set) render without a group header
- [ ] Verify separator appears between ungrouped and grouped sections when both exist
- [ ] Verify searchable Select still works correctly with groups and separators (empty groups hidden when filtered)
- [ ] Verify List, Toggle, Radio, and Slider variants are unaffected
